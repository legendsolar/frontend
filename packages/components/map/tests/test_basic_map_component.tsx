import {BasicMap} from '../basic_map_component';

export default () => (
    <BasicMap
        lat={41.375094}
        lng={-74.692663}
        width={'100vw'}
        height={'100vh'}
        zoom={8}
    ></BasicMap>
);
