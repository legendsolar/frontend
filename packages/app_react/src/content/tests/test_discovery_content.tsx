import DiscoveryContent from './discovery_content';
import assetData from '../static/assets';

export default () => (
    <DiscoveryContent
        assets={assetData}
        onAssetClick={() => {}}
    ></DiscoveryContent>
);
