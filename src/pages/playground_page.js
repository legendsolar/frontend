import { Paper, Typography, Stack, Button } from "@mui/material";
import DefaultView from "../views/default_view";

const PlaygroundPage = () => {
    return (
        <DefaultView>
            <Paper variant="container"></Paper>
        </DefaultView>
    );
};

export default PlaygroundPage;
