import { Typography } from "@mui/material";

const Footer = (props) => {
    return (
        <Box>
            <Typography>This is an alpha view of the Legends App.</Typography>
        </Box>
    );
};

export default Footer;
